[workspace]
members = ["dockq-core", "dockq-cli", "dockq-py"]
default-members = ["dockq-core", "dockq-cli"]
resolver = "2"

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "MIT"
authors = ["dockq-rs"]

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
