//! dockq-rs CLI (task #7). Will match the reference DockQ CLI output byte-for-byte
//! (header aside), including --short/--json/--mapping/--capri_peptide/--no_align/
//! --allowed_mismatches/--n_cpu, plus a batch subcommand.

fn main() {
    eprintln!("dockq-rs CLI not yet implemented (task #7)");
    std::process::exit(2);
}
